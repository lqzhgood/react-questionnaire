import React from 'react';
import ThemeButton from './ThemeButton';

const Toolbar = () => {
    return (
        <div>
            <p>Toolbar</p>
            <ThemeButton />
        </div>
    );
};

export default Toolbar;
